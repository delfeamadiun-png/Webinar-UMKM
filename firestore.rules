rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Default-allow block for debugging, permitting anyone to read and write any data
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
